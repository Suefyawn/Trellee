# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# design source bundle (kept locally for reference, not committed)
/.design

# uploads (dev only)
/public/uploads

# claude personal config (project-wide claude config can be tracked, but
# the local settings/permissions file is per-machine)
/.claude/settings.local.json
